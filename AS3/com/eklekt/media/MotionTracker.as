package com.eklekt.media {	import com.soulwire.media.MotionTracker;	import flash.media.Video;		import flash.geom.Rectangle;	import flash.external.ExternalInterface; 	import flash.events.Event; 	public class MotionTracker extends com.soulwire.media.MotionTracker	{				/*		========================================================		| Constructor		========================================================		*/				/**		 * The MotionTracker class will track the movement within video data		 * 		 * @param	source	A video object which will be used to track motion		 */				public function MotionTracker( source:Video ) 		{			super(source);						/*            if (ExternalInterface.available) {                try {                    ExternalInterface.addCallback("callAS", handleJS);                } catch (error:SecurityError) {                    //output.appendText("A SecurityError occurred: " + error.message + "\n");                } catch (error:Error) {                    //output.appendText("An Error occurred: " + error.message + "\n");                }            }			*/					}				public function get centerOfMovement():Array {			var area:Rectangle = this.motionArea;						var centerX:int = area.x + (area.width / 2);			var centerY:int = area.y + (area.height / 2);						var coordinates:Array = new Array(2);			coordinates[0] = centerX;			coordinates[1] = centerY;						return coordinates;		}				/*		public function handleJS(val:String):void {			if (val == "getCenterOfMovement") {				return this.centerOfMovement;			}		}		*/	}				}